import * as React from 'react';
import { FloatingDropdown } from '..';

export const FloatingDropdownExtended = () => {
  return <FloatingDropdown onChange={() => {}} value={'1'} label={'Label'} />;
};
