import * as React from 'react';
import { TextButton } from '../TextButton';
import { classes } from './TextButtonExtended.st.css';

export const TextButtonExtended = () => (
  <TextButton className={classes.root}>Text</TextButton>
);
