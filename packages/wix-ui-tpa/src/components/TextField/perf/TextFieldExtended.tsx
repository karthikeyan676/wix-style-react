import * as React from 'react';
import { TextField } from '../TextField';
import { classes } from './TextFieldExtended.st.css';

export const TextFieldExtended = () => <TextField className={classes.root} />;
